import Express from "express";

const FriendAPIController = {
  check: async (req: Express.Request, res: Express.Response) => {},
  accept: async (req: Express.Request, res: Express.Response) => {},
  decline: async (req: Express.Request, res: Express.Response) => {},
  request: async (req: Express.Request, res: Express.Response) => {},
};

export default FriendAPIController;
